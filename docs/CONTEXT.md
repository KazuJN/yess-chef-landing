# Project context (for Cursor)

**Project path:** `~/Cursor/yess-chef-landing`  
**Paper file:** “Xanthic moon” (Yess Chef restaurant landing — v1 had nav, hero, booking/hours)

## Done so far

- Next.js 15 + TypeScript + Tailwind v4 scaffold
- Sections: `SiteHeader`, `Hero`, `BookingSection`; shared `Button`, `Container`, `Divider`
- Assets in `public/images/` (hero, booking cat, background pattern); pattern opacity ~0.22
- Node via nvm (`~/.zshrc`); `npm install` / `npm run dev` at localhost:3000

## Paused — you are redesigning in Paper

- Mobile-first: artboards **390**, **768**, **1440** with matching layer names
- Full polish plan: **[docs/plan.md](./plan.md)** (Lenis, GSAP, Supabase CDN, videos, tokens)

## Resume in Cursor

1. Open folder `~/Cursor/yess-chef-landing`
2. New or restored chat → **@docs/plan.md** + **@docs/CONTEXT.md**
3. Say: “Paper breakpoints are ready — implement the plan from Phase 1” (or per section)

## Chat history note

Agent transcripts were merged from the old `~/Projects/yess-chef-landing` workspace into Cursor’s project data. **Restart Cursor** and use the history (clock) icon if threads don’t appear immediately.
